class Formatter {

  // TODO: add validateArgs() when parser is implemented

  apply(args, vars, ctx) {
  }
}

class Predicate {

  // TODO: add validateArgs() when parser is implemented

  apply(args, ctx) {
  }

}

export {
  Formatter,
  Predicate,
};
